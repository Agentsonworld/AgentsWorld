services:
  - type: web
    name: agentsworld
    env: node
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /v1/health
    autoDeploy: true
    envVars:
      - key: NODE_VERSION
        value: 20
